import type { Metadata } from "next";
import { SharedStakeholderPlanLoader } from "@/components/plansight-ai/SharedStakeholderPlanLoader";

export const runtime = "nodejs";

type Props = {
  params: Promise<{ shareId: string }>;
};

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { shareId } = await params;

  return {
    title: `Shared Plan | ${shareId}`,
    description: "Read-only stakeholder view generated by PlanSight AI."
  };
}

export default async function SharePage({ params }: Props) {
  const { shareId } = await params;

  return (
    <main className="min-h-screen bg-light">
      <section className="border-b border-slate-200 bg-white">
        <div className="mx-auto flex max-w-[1600px] flex-col gap-3 px-6 py-8">
          <div className="flex flex-wrap items-center gap-3">
            <p className="text-sm font-semibold uppercase tracking-normal text-primary">
              PlanSight AI
            </p>
            <span className="text-sm text-slate-400">powered by AI Solution Maven</span>
          </div>
          <div className="flex flex-wrap items-center gap-3 text-sm text-slate-600">
            <span className="font-medium text-dark">Shared plan</span>
            <span className="text-slate-400">·</span>
            <span>Read-only stakeholder view</span>
            <span className="text-slate-400">·</span>
            <span>/share/{shareId}</span>
          </div>
        </div>
      </section>

      <SharedStakeholderPlanLoader shareId={shareId} />
    </main>
  );
}
